import 'package:flutter/material.dart';

class DeveloperPath extends StatelessWidget {
  static const route = 'developer-path';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: Column(
        children: <Widget>[],
      )),
    );
  }
}

List<Map<String, String>> data = [
  {'question': 'answer'},
  {'question': 'answer'}
];
